---
marp: true
theme: charcoal-lb
paginate: true
---

<!-- _class: lead -->
<!-- _paginate: false -->

# Chapter 2
## Variables, numbers, and modules

IS 640 — Business Application Programming

---

<!-- _class: lead -->

# 2.1 Variables and Assignments

---

## Variable

Named item, such as x, y, john, lamp.

**Assignment statement:**
We assign variables with `=`.

```python
x = 13
JohnDoe = 55
```

<!--
Ask: what would you name a variable holding someone's salary? Get a few
answers before moving to the rules slide.
-->

---

## The rules

<div class="columns">
<div>

## Do this
`x = 13` ✅
`JohnDoe = 55` ✅

</div>
<div>

## Not this
`5 = x` ❌
`x => 5` ❌
`1x = 5` ❌

</div>
</div>

Variable always starts on the left, then `=`, then the value.

---

## Naming styles

- `lower_case_with_underscores` — our default
- `UPPER_CASE` — constants
- `CapitalizedWords` — classes later in the semester
- `mixedCase` ⚠️ avoid unless already common in the script
- `Capitalized_Words_With_Underscores` ❌ ugly, don't

<!--
This is a PEP 8 preview. We will come back to naming conventions all
semester — no need to dwell here.
-->

---

<!-- _class: lead -->

# 2.2 Identifiers

---

## Identifier

A sequence of letters, underscores, and digits. Must start with a letter or
underscore.

Python is **case sensitive** — `Cat` and `cat` are different names.

---

## Reserved words

Python reserves words to do its own work. Don't overwrite them.

```python
print = 13
sum = 13 + 23
```

Both ❌ — you just broke `print()` and `sum()` for the rest of your session.

<!--
Live demo: overwrite print, then try to use it, then restart the kernel to
fix it. Good scare tactic early in the semester.
-->

---

## Finding the full list

```python
import keyword
import builtins

keyword_list = keyword.kwlist
builtin_list = dir(builtins)
reserved_words = keyword_list + builtin_list
print(f'Number of words to avoid: {len(reserved_words)}')
```

<!--
We haven't covered import yet — tell them to trust it for now, 2.8 explains
what's happening here.
-->

---

<!-- _class: lead -->

# 2.3 Objects

---

## Every value is an object

An object represents a value, created automatically by the interpreter.

```python
x = 4
```

That line creates an object to represent `4`, and binds the name `x` to it.

---

## Name binding

An object can have more than one name bound to it. Every name is bound to
exactly one object.

```python
x = '🦖'

# Lets show the value
print(x)

# Lets show the type
print(type(x))

# Lets show the identity
print(id(x))
```

<!--
print(), type(), id() are all built in — no import needed. Worth pointing
out since 2.8 introduces the ones that DO need an import.
-->

---

## Three properties, every object

**Value:** what it holds — `'Hi Im Baby'`, `13`, `'13'`

**Type:** its format — int, string, etc.

**Identity:** a unique id, assigned by the interpreter

<!--
Garbage collection is the reason identity isn't forever — Python frees
unused objects automatically once nothing points at them.
-->

---

<!-- _class: lead -->

# 2.4 Numeric Types

---

## Floating-point numbers

A real number like `13.01`, `-55.23`, `0.00001`.

A **float literal** always shows the fractional part, even if it's `0`:
`1.0`, `0.0`, `99.0`.

```python
float('13.5')
int('13')
```

Both convert if they can — neither can convert `'Hi'`.

---

## Scientific notation

`0.003` is the same as `3.0e-3`

- Negative exponent moves the decimal left ⬅️
- Positive exponent moves the decimal right ➡️

---

## Overflow

Floats have a limit to how long a decimal can get.

**Overflow error:**
A value too large to be stored in the memory the interpreter allocated.

32-bit Python tops out around `1.0e308`. Yes, that's 307 zeros.

---

## Formatting floats

```python
print(f'{13.12345: .2f}')
```

Output: `13.12`

Nobody wants to read 13 decimal places in a report. Clean it up.

<!--
No commas allowed inside a number literal — 13,000 is not valid Python.
Worth a quick warning since Excel trains people to expect commas.
-->

---

<!-- _class: lead -->

# 2.5 Arithmetic Expressions

---

## Expression

A combination of variables, literals, operators, and parentheses that
evaluates to a value — like `2 * (x + 1)`.

| Operator | Meaning |
|-|-|
| `+` `-` | add, subtract (and unary negation) |
| `*` | multiply |
| `/` | divide |
| `**` | exponent |

---

## Order of operations

Same PEMDAS you already know:

1. `( )` parentheses
2. `**` exponent
3. unary `-`
4. `*` `/` `%` left to right
5. `+` `-` left to right

`**` alone evaluates right to left.

<!--
Ask the room to predict the output of `2 + 3 * 4` before you run it — most
will get it right, which is the point: this is arithmetic they already know.
-->

---

## Try it

```python
1 + 0
1 * 0
0 / 1
5 // 2
2 ** 4
4 % 2
(1 + 3) * (2 + 2)
```

<!--
Run these one at a time. // and % get their own section next — don't
over-explain them here, just flag that they're coming.
-->

---

<!-- _class: lead -->

# 2.6 Python Expressions

---

## A realistic expression

```python
down_payment = int(input('Enter down payment: '))
payment_per_month = int(input('Enter monthly payment: '))
num_months = int(input('Enter number of months: '))

total_cost = down_payment + (payment_per_month * num_months)
print(f'Total cost: ${total_cost:.2f}')
```

<!--
This is a car lease calculator. Run it live and put in real numbers — the
class always has opinions about car payments.
-->

---

## Compound operators

Shorthand for "update this variable using itself."

| Compound | Expands to |
|-|-|
| `age += 1` | `age = age + 1` |
| `age -= 1` | `age = age - 1` |
| `age *= 1` | `age = age * 1` |
| `age /= 1` | `age = age / 1` |
| `age %= 1` | `age = age % 1` |

Write it the long way until it clicks, then switch.

---

<!-- _class: lead -->

# 2.7 Division & Modulo

---

## Three kinds of division

Input: `4 / 2`
Output: `2.0`

Regular division always returns a **float**, even with no remainder.

---

## Floor division and modulo

**Floor division `//`:** rounds down to the smallest whole number.

Input: `5 // 3`
Output: `1`

**Modulo `%`:** the remainder left over.

Input: `5 % 3`
Output: `2`

<!--
Business framing: modulo is how you tell if a number is even (n % 2 == 0)
or split a total into equal groups with a remainder.
-->

---

<!-- _class: lead -->

# 2.8 Module Basics

---

## .py vs .ipynb

<div class="columns">
<div>

## .py
- Plain script file
- Run via CLI: `python file.py`
- Or the play button in VS Code

</div>
<div>

## .ipynb
- Jupyter notebook, cell by cell
- Better for exploring and visualizing
- Common at Databricks / Azure Notebooks

</div>
</div>

<!--
Ask: if your company runs a nightly scheduled job on a server, which format
do you reach for? .py — schedulers execute scripts, not notebooks.
-->

---

## Module and import

**Module:** a file containing Python code other files can reuse.

**Import:** how you make a module available in your script.

```python
import MyFirstFunction

MyFirstFunction.bmi_calculator()
```

Dot notation reaches into whatever the module defines.

---

## `__name__ == '__main__'`

```python
# Is this the main file we are running or is it an import?
if __name__ == '__main__':
    # If this is the main file, run the meat and potatoes
    main()
else:
    # If it's imported, only run this
    some_other_function()
```

Importing a module runs the whole file — unless you guard it like this.

<!--
This trips everyone up the first time they import a script and watch it
print output they didn't ask for. Worth a live demo.
-->

---

<!-- _class: lead -->

# 2.9 Math Module

---

## import math

```python
import math

num = 144
sqrt_num = math.sqrt(num)
print(sqrt_num)
```

Output: `12.0`

`sqrt()`, like `print()`, is a **function** — a named, reusable block of
statements.

---

## A few you'll actually use

| Function | Does |
|-|-|
| `math.sqrt(x)` | square root |
| `math.ceil(x)` / `math.floor(x)` | round up / down |
| `math.factorial(x)` | x! |
| `math.pi` | 3.14159... |

<!--
Point out dir(math) as the way to self-serve the full list instead of
memorizing it — that's the real skill, not the function names.
-->

---

<!-- _class: lead -->

# 2.10 Random Numbers

---

## import random

```python
import random

# Lets generate a random floating point number
print(random.random())
```

Not actually random — **pseudo-random**, generated from a seed.

Same seed, same sequence, every time. Useful for reproducible demos.

---

## Ranges you control

```python
random.randrange(3)          # 0, 1, or 2
random.randint(12, 20)       # 12 through 20, inclusive
random.randrange(12, 20)     # 12 through 19
```

<!--
Ask the room which one they'd reach for to simulate a dice roll
(randint(1, 6)) vs a random index into a list (randrange).
-->

---

<!-- _class: lead -->

# 2.11 Representing Text

---

## chr() and ord()

```python
for num in range(32, 127):
    char_converted = chr(num)
    print(f'{num}: {char_converted}')
```

`chr()` turns a number into a character. `ord()` does the reverse.

---

## Escape sequences

The backslash tells Python "don't read the next character literally."

| Sequence | Meaning |
|-|-|
| `\n` | newline |
| `\t` | tab |
| `\\` | a literal backslash |
| `\'` | a literal single quote |

```python
print('Hello, this is the first line\nThis is the second line')
```

---

## Raw strings

```python
my_string = 'This is a \n \'normal\' string\n'
my_raw_string = r'This is a \n \'raw\' string'
```

The `r` prefix tells Python to ignore every escape sequence in the string.

<!--
Useful heads-up for file paths on Windows — r'C:\Users\name' saves you from
escaping every backslash.
-->

---

<!-- _class: lead -->

# Questions?

Notes for every section live on the course site.
